# Copy this file to ".env" and adjust if your setup differs.
# .env holds your real password, so never commit it to GitHub.

PGHOST=localhost
PGPORT=5432
PGDATABASE=bridgeapp
PGUSER=bridgeapp_app
PGPASSWORD=bridge_demo_2026

PORT=4000

# Signs login tokens. Any long random string. Change it for anything real.
JWT_SECRET=change-this-to-a-long-random-string
